resources:
  experiments:
    experiment:
      name: ${resources.volumes.model_storage_location_volume.volume_path}/experiments/${var.ml_model_name}
      permissions:
        - level: CAN_READ
          group_name: workspace_users
      artifact_location: dbfs:${resources.volumes.model_storage_location_volume.volume_path}/experiments