resources:
  pipelines:
    ames_housing_pipeline:
      name: ames_housing_pipeline
      serverless: true
      channel: "PREVIEW"
      catalog: ${var.catalog}
      schema: ${resources.schemas.ames_housing_schema.name}
      root_path: "."
      libraries:
        - glob:
            include: transformations/**
