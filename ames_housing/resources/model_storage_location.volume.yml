resources:
  volumes:
    model_storage_location_volume:
      catalog_name: ${var.catalog}
      name: ${var.model_storage_location_volume}
      schema_name: ${resources.schemas.ames_housing_schema.name}