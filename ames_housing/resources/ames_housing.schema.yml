resources:
  schemas:
    ames_housing_schema:
      name: ${var.schema}
      grants:
        - principal: users
          privileges:
            - USE
            - SELECT
            - READ_VOLUME
            - EXECUTE
        - principal: ${var.run_as_user}
          privileges:
            - MANAGE
            - ALL_PRIVILEGES
      catalog_name: ${var.catalog}