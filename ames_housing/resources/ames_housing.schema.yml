resources:
  schemas:
    ames_housing_schema:
      name: ${var.schema}
      grants:
        - principal: users
          privileges:
            - SELECT
        - principal: ${var.run_as_user}
          privileges:
            - CAN_MANAGE
      catalog_name: ${var.catalog}