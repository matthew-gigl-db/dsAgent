resources:
  schemas:
    ames_housing_schema:
      name: ${var.schema}
      grants:
        - principal: workspace_users
          privileges:
            - SELECT
        - principal: ${var.run_as_user}
          privileges:
            - MANAGE
      catalog_name: ${var.catalog}